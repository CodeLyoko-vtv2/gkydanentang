# 📱 Study Notes - Ứng dụng Ghi chú học tập

## 👨‍🎓 Thông tin sinh viên

- **Họ và tên**: Đinh Lê Thái Dương
- **MSSV**: 22IT056
- **Lớp**: 22SE2
- **Đề tài**: Đề 4 - Ứng dụng Ghi chú học tập (Study Notes)

## 📋 Mô tả dự án

Ứng dụng Study Notes được xây dựng bằng **Ionic Framework + Capacitor 7** với **React** và **TypeScript**. Ứng dụng cho phép người dùng ghi chú theo từng môn học và lưu trữ dữ liệu cục bộ sử dụng **Capacitor Preferences API**.

## ✨ Tính năng chính

### 1. Màn hình chính (Home)

- ✅ Hiển thị danh sách các môn học: Toán, Lý, Hóa, Anh, CNTT
- ✅ Giao diện đẹp mắt với gradient header
- ✅ Icon và mô tả cho mỗi môn học
- ✅ Điều hướng sang màn hình ghi chú khi chọn môn

### 2. Màn hình ghi chú (Notes)

- ✅ Nhận tham số `subject` từ URL
- ✅ Hiển thị danh sách ghi chú của môn học đó
- ✅ Form nhập ghi chú mới với validation
- ✅ Thêm ghi chú mới (Enter hoặc nút Thêm)
- ✅ Xóa ghi chú bằng cách vuốt sang trái
- ✅ Tự động lưu và reload danh sách
- ✅ Toast notifications cho các thao tác
- ✅ Empty state khi chưa có ghi chú

### 3. Lưu trữ dữ liệu

- ✅ Sử dụng `@capacitor/preferences` (chính thức từ Capacitor 7)
- ✅ Lưu theo format: `notes_<subject>` với giá trị JSON string
- ✅ Service class `NotesStorage` để quản lý
- ✅ Hỗ trợ CRUD operations đầy đủ

## 🛠️ Công nghệ sử dụng

- **Framework**: Ionic 8.5.0
- **Capacitor**: 7.4.4
- **React**: 19.0.0
- **TypeScript**: 5.1.6
- **React Router**: 5.3.4
- **Storage**: @capacitor/preferences 7.0.2

## 📦 Cài đặt và Chạy dự án

### 1. Cài đặt dependencies

```bash
npm install
```

### 2. Cài đặt Capacitor Preferences (đã có trong package.json)

```bash
npm install @capacitor/preferences
```

### 3. Build project

```bash
npm run build
```

### 4. Sync với Capacitor

```bash
npx cap sync
```

### 5. Chạy trên trình duyệt (Development)

```bash
ionic serve
```

hoặc

```bash
npm run dev
```

Ứng dụng sẽ chạy tại: http://localhost:5173

### 6. Chạy trên Android

#### Mở Android Studio:

```bash
npx cap open android
```

#### Hoặc chạy trực tiếp:

```bash
npx cap run android
```

**Lưu ý**: Cần có Android Studio và Android SDK đã cài đặt.

### 7. Chạy trên iOS (chỉ trên macOS)

```bash
npx cap open ios
```

**Lưu ý**: Cần có Xcode đã cài đặt.

## 📁 Cấu trúc thư mục

```
study-notes/
├── android/                    # Android native project
├── ios/                        # iOS native project
├── public/                     # Static assets
│   └── manifest.json
├── src/
│   ├── components/            # React components
│   ├── data/
│   │   └── subjects.ts        # Danh sách môn học
│   ├── pages/
│   │   ├── Home.tsx           # Màn hình chính
│   │   ├── Home.css           # Styles cho Home
│   │   ├── Notes.tsx          # Màn hình ghi chú
│   │   └── Notes.css          # Styles cho Notes
│   ├── services/
│   │   └── notesStorage.ts    # Service quản lý storage
│   ├── theme/
│   │   └── variables.css      # Theme variables
│   ├── App.tsx                # Main app component
│   ├── App.test.tsx           # App tests
│   ├── main.tsx               # Entry point
│   └── vite-env.d.ts          # Type definitions
├── screenshots/               # Ảnh chụp màn hình
│   └── README.md              # Hướng dẫn chụp screenshots
├── capacitor.config.ts        # Capacitor configuration
├── ionic.config.json          # Ionic configuration
├── package.json               # Dependencies
├── tsconfig.json              # TypeScript config
├── vite.config.ts             # Vite config
└── README.md                  # File này
```

## 🎨 Giao diện

### Home Screen

- Header gradient với màu primary và secondary
- Danh sách môn học dạng cards
- Icons đẹp mắt từ Ionicons
- Responsive design

### Notes Screen

- Back button về Home
- Form thêm ghi chú với input và button
- Danh sách ghi chú với numbering
- Swipe to delete functionality
- Empty state thân thiện
- Toast notifications

## 🔧 API Reference

### NotesStorage Service

```typescript
class NotesStorage {
  // Lấy danh sách ghi chú của môn học
  static async getNotes(subject: string): Promise<string[]>
  
  // Lưu danh sách ghi chú
  static async saveNotes(subject: string, notes: string[]): Promise<void>
  
  // Xóa toàn bộ ghi chú của môn học
  static async clearNotes(subject: string): Promise<void>
}
```

### Cách sử dụng

```typescript
import { NotesStorage } from '../services/notesStorage';

// Lấy ghi chú
const notes = await NotesStorage.getNotes('Toán');

// Lưu ghi chú
await NotesStorage.saveNotes('Toán', ['Bài 1', 'Bài 2']);

// Xóa ghi chú
await NotesStorage.clearNotes('Toán');
```

## 📱 Build cho Production

### Android APK

```bash
# Build web assets
npm run build

# Sync với Capacitor
npx cap sync android

# Mở Android Studio
npx cap open android
```

Trong Android Studio:

1. Chọn **Build > Build Bundle(s) / APK(s) > Build APK(s)**
2. Đợi build hoàn tất
3. APK sẽ được tạo trong `android/app/build/outputs/apk/`

### iOS App

```bash
# Build web assets
npm run build

# Sync với Capacitor
npx cap sync ios

# Mở Xcode
npx cap open ios
```

Trong Xcode:

1. Chọn device/simulator
2. Chọn **Product > Archive**
3. Distribute app

## 🧪 Testing

### Unit Tests

```bash
npm run test.unit
```

### E2E Tests (Cypress)

```bash
npm run test.e2e
```

## 📸 Screenshots

Xem folder `screenshots/` để xem hình ảnh minh họa ứng dụng.

## ⚠️ Lưu ý quan trọng

1. **KHÔNG sử dụng** `@capacitor/storage` (đã deprecated)
2. **CHỈ dùng** `@capacitor/preferences` (official plugin từ Capacitor 7)
3. Dữ liệu được lưu local, không sync cloud
4. Mỗi môn học có storage key riêng: `notes_<subject>`
5. Dữ liệu được lưu dạng JSON string

## 🐛 Troubleshooting

### Lỗi: Cannot find module '@capacitor/preferences'

```bash
npm install @capacitor/preferences
npx cap sync
```

### Lỗi: Android build failed

```bash
cd android
./gradlew clean
cd ..
npx cap sync android
```

### Lỗi: iOS build failed

```bash
cd ios/App
pod install
cd ../..
npx cap sync ios
```

## 📚 Tài liệu tham khảo

- [Ionic Documentation](https://ionicframework.com/docs)
- [Capacitor Documentation](https://capacitorjs.com/docs)
- [Capacitor Preferences API](https://capacitorjs.com/docs/apis/preferences)
- [React Documentation](https://react.dev)

## 📄 License

This project is created for educational purposes (Midterm Project).

## 👨‍💻 Author

[Nhập tên của bạn] - [Nhập email của bạn]

---

**Made with ❤️ using Ionic + Capacitor 7**
